--- before
+++ after
@@ -6704,13 +6704,13 @@
 }
 function buildProgressSummaryCard(options = {}) {
   const p = getProgressSnapshot();
-  const note = p.complete ? 'Parcours terminé. Vous pouvez commencer un nouveau cycle tout en conservant vos surlignages et notes.' : (p.count ? 'Votre progression est enregistrée sur cet appareil. Vous pouvez poursuivre quand vous le souhaitez.' : 'Aucune Heure n’est encore marquée comme lue.');
-  const actionButtons = options.withActions === false ? '' : `<div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showCompletedHoursView()">Revoir les Heures terminées</button>${p.hasCycleState ? `<button type="button" class="resume-panel-btn" onclick="restartTwentyFourHours()">${p.complete ? 'Recommencer les 24 Heures' : 'Réinitialiser ma progression'}</button>` : ''}</div>`;
-  return `<div class="progress-summary-card"><div class="progress-summary-top"><div class="progress-summary-count">${p.count}/24</div><div class="progress-summary-label">${p.pct}%<br>${p.complete ? 'Parcours terminé' : 'Heures marquées comme lues'}</div></div><div class="progress-summary-track" aria-label="Progression ${p.count} sur 24"><div class="progress-summary-fill" style="width:${p.pct}%"></div></div><div class="progress-summary-note">${note}</div>${actionButtons}</div>`;
+  const note = p.complete ? 'Les 24 Heures sont marquées comme méditées. Vous pouvez recommencer depuis la 1re Heure tout en conservant vos surlignages et notes.' : (p.count ? 'Votre progression est enregistrée sur cet appareil. Vous pouvez poursuivre quand vous le souhaitez.' : 'Aucune Heure n’est encore marquée comme méditée.');
+  const actionButtons = options.withActions === false ? '' : `<div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showCompletedHoursView()">Revoir les Heures méditées</button>${p.hasCycleState ? `<button type="button" class="resume-panel-btn" onclick="restartTwentyFourHours()">${p.complete ? 'Recommencer depuis la 1re Heure' : 'Réinitialiser ma progression'}</button>` : ''}</div>`;
+  return `<div class="progress-summary-card"><div class="progress-summary-top"><div class="progress-summary-count">${p.count}/24</div><div class="progress-summary-label">${p.pct}%<br>${p.complete ? 'Cycle accompli' : 'Heures marquées comme méditées'}</div></div><div class="progress-summary-track" aria-label="Progression ${p.count} sur 24"><div class="progress-summary-fill" style="width:${p.pct}%"></div></div><div class="progress-summary-note">${note}</div>${actionButtons}</div>`;
 }
 function getCompletedHoursHtml() {
   const completed = getCompletedHourNumbers();
-  if (!completed.length) return '<div class="progress-empty">Aucune Heure terminée pour le moment. Marquez une Heure comme lue lorsque vous souhaitez l’ajouter à votre progression.</div>';
+  if (!completed.length) return '<div class="progress-empty">Aucune Heure n’est encore marquée comme méditée. Appuyez sur « Méditée » après avoir médité une Heure pour l’ajouter à votre progression.</div>';
   return `<div class="progress-hours-grid">${completed.map(n => { const h = CORPUS.hours.find(x => x.hour_number === n); return h ? `<button type="button" class="progress-hour-btn" onclick="openHour(${n})"><span class="progress-hour-num">${n === 1 ? '1re' : n + 'e'} Heure</span><span class="progress-hour-title">${escHtml(h.short_title || h.title)}</span></button>` : ''; }).join('')}</div>`;
 }
 function showProgressView(push = true) {
@@ -6718,7 +6718,7 @@
   state.view = 'progress'; state.currentHour = null; state.currentSection = null; state.currentPrayer = null;
   resetContentScroll();
   const p = getProgressSnapshot();
-  document.getElementById('content').innerHTML = `<div class="espace-screen"><div class="library-hero"><div class="library-eyebrow">Espace personnel</div><h2 class="library-title">Ma progression</h2><p class="library-subtitle">Retrouvez les Heures marquées comme lues et recommencez un cycle lorsque vous le souhaitez.</p></div>${buildProgressSummaryCard({withActions:false})}<div class="espace-section"><div class="espace-section-title">Heures terminées</div>${getCompletedHoursHtml()}</div><div class="espace-section"><div class="espace-section-title">Parcours</div><div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showCompletedHoursView()">Revoir les Heures terminées</button><button type="button" class="resume-panel-btn" onclick="showHome()">Revenir à l’Accueil</button>${p.hasCycleState ? `<button type="button" class="resume-panel-btn" onclick="restartTwentyFourHours()">${p.complete ? 'Recommencer les 24 Heures' : 'Réinitialiser ma progression'}</button>` : ''}</div></div></div>`;
+  document.getElementById('content').innerHTML = `<div class="espace-screen"><div class="library-hero"><div class="library-eyebrow">Espace personnel</div><h2 class="library-title">Ma progression</h2><p class="library-subtitle">Retrouvez les Heures marquées comme méditées et gérez votre progression.</p></div>${buildProgressSummaryCard({withActions:false})}<div class="espace-section"><div class="espace-section-title">Heures méditées</div>${getCompletedHoursHtml()}</div><div class="espace-section"><div class="espace-section-title">Parcours</div><div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showCompletedHoursView()">Revoir les Heures méditées</button><button type="button" class="resume-panel-btn" onclick="showHome()">Revenir à l’Accueil</button>${p.hasCycleState ? `<button type="button" class="resume-panel-btn" onclick="restartTwentyFourHours()">${p.complete ? 'Recommencer depuis la 1re Heure' : 'Réinitialiser ma progression'}</button>` : ''}</div></div></div>`;
   document.getElementById('progressWrap').style.display = 'none';
   setBottomNav('espace'); buildSidebar(); updateRightContextPanel();
 }
@@ -6726,7 +6726,7 @@
   if (push) pushHistory(state.view, getHistoryDataForView(state.view));
   state.view = 'completedHours'; state.currentHour = null; state.currentSection = null; state.currentPrayer = null;
   resetContentScroll();
-  document.getElementById('content').innerHTML = `<div class="espace-screen"><div class="library-hero"><div class="library-eyebrow">Ma progression</div><h2 class="library-title">Heures terminées</h2><p class="library-subtitle">Relisez une Heure déjà marquée comme lue ou revenez à votre progression.</p></div>${buildProgressSummaryCard({withActions:false})}<div class="espace-section">${getCompletedHoursHtml()}</div><div class="espace-section"><div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showProgressView()">Voir ma progression</button><button type="button" class="resume-panel-btn" onclick="showHome()">Revenir à l’Accueil</button></div></div></div>`;
+  document.getElementById('content').innerHTML = `<div class="espace-screen"><div class="library-hero"><div class="library-eyebrow">Ma progression</div><h2 class="library-title">Heures méditées</h2><p class="library-subtitle">Relisez une Heure déjà marquée comme méditée ou revenez à votre progression.</p></div>${buildProgressSummaryCard({withActions:false})}<div class="espace-section">${getCompletedHoursHtml()}</div><div class="espace-section"><div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showProgressView()">Voir ma progression</button><button type="button" class="resume-panel-btn" onclick="showHome()">Revenir à l’Accueil</button></div></div></div>`;
   document.getElementById('progressWrap').style.display = 'none';
   setBottomNav('espace'); buildSidebar(); updateRightContextPanel();
 }
@@ -6908,7 +6908,7 @@
   document.getElementById('content').innerHTML = `<div class="espace-screen">
     ${lastH ? `<button type="button" class="espace-resume-hero" onclick="openHour(${lastH.hour_number}, true, { resume: true })"><div class="erh-inner"><div class="erh-label">Reprendre</div><div class="erh-title">${lastH.hour_number}e Heure — ${escHtml(lastH.title)}</div><div class="erh-meta">${escHtml(describeSavedPara(getSavedParaForHour(lastH.hour_number)))} · ${escHtml(lastH.display_time)}</div></div><svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="flex-shrink:0;color:var(--accent)"><polyline points="9 18 15 12 9 6"/></svg></button>` : ''}
     <div class="library-hero" style="margin-top:${lastH ? '0.25rem' : '0'}"><div class="library-eyebrow">Espace personnel</div><h2 class="library-title">Mon Espace</h2></div>
-    <div class="espace-section"><div class="espace-section-title"><span>Progression</span><span class="espace-section-count">${state.readHours.size}/24 Heures</span></div>${buildProgressSummaryCard({withActions:false})}<div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showProgressView()">Voir ma progression</button><button type="button" class="resume-panel-btn" onclick="showCompletedHoursView()">Revoir les Heures terminées</button>${progress.hasCycleState ? `<button type="button" class="resume-panel-btn" onclick="restartTwentyFourHours()">${progress.complete ? 'Recommencer les 24 Heures' : 'Réinitialiser ma progression'}</button>` : ''}</div></div>
+    <div class="espace-section"><div class="espace-section-title"><span>Progression</span><span class="espace-section-count">${state.readHours.size}/24 Heures</span></div>${buildProgressSummaryCard({withActions:false})}<div class="progress-actions"><button type="button" class="resume-panel-btn primary" onclick="showProgressView()">Voir ma progression</button><button type="button" class="resume-panel-btn" onclick="showCompletedHoursView()">Revoir les Heures méditées</button>${progress.hasCycleState ? `<button type="button" class="resume-panel-btn" onclick="restartTwentyFourHours()">${progress.complete ? 'Recommencer depuis la 1re Heure' : 'Réinitialiser ma progression'}</button>` : ''}</div></div>
     <div class="espace-section"><div class="espace-section-title"><span>Passages à vérifier</span><span class="espace-section-count">${staleEntries.length}</span></div>${staleHtml}</div>
     <div class="espace-section"><div class="espace-section-title"><span>Lectures marquées</span><span class="espace-section-count">${markedReadings.length}</span></div>${markedReadingsHtml}</div>
     <div class="espace-section"><div class="espace-section-title"><span>Surlignages</span><span class="espace-section-count">${freshEntries.length}</span></div>${hlHtml}</div>
