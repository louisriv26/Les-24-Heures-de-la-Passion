# v101.130 predecessor-audit classification

`fresh_v101129_runtime/` is deliberately retained as **predecessor audit evidence** showing why v101.130 was created. Its internal `v101.129` labels are historical/test-target labels, not current v101.130 package claims.

Current v101.130 runtime evidence is under `current_runtime/` and is bound to the v101.130 HTML SHA recorded in `04_PASS2_RUNTIME_PACKAGE_BEHAVIOUR_SUMMARY.json`.
